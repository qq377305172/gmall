<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall.dao.PmsCommentReplayDao">

    <resultMap type="com.atguigu.gmall.entity.PmsCommentReplay" id="PmsCommentReplayMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="commentId" column="comment_id" jdbcType="INTEGER"/>
        <result property="memberNickName" column="member_nick_name" jdbcType="VARCHAR"/>
        <result property="memberIcon" column="member_icon" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PmsCommentReplayMap">
        select
          id, comment_id, member_nick_name, member_icon, content, create_time, type
        from gmall.pms_comment_replay
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PmsCommentReplayMap">
        select
          id, comment_id, member_nick_name, member_icon, content, create_time, type
        from gmall.pms_comment_replay
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="PmsCommentReplayMap">
        select
          id, comment_id, member_nick_name, member_icon, content, create_time, type
        from gmall.pms_comment_replay
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="commentId != null">
                and comment_id = #{commentId}
            </if>
            <if test="memberNickName != null and memberNickName != ''">
                and member_nick_name = #{memberNickName}
            </if>
            <if test="memberIcon != null and memberIcon != ''">
                and member_icon = #{memberIcon}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="type != null">
                and type = #{type}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into gmall.pms_comment_replay(comment_id, member_nick_name, member_icon, content, create_time, type)
        values (#{commentId}, #{memberNickName}, #{memberIcon}, #{content}, #{createTime}, #{type})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update gmall.pms_comment_replay
        <set>
            <if test="commentId != null">
                comment_id = #{commentId},
            </if>
            <if test="memberNickName != null and memberNickName != ''">
                member_nick_name = #{memberNickName},
            </if>
            <if test="memberIcon != null and memberIcon != ''">
                member_icon = #{memberIcon},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from gmall.pms_comment_replay where id = #{id}
    </delete>

</mapper>